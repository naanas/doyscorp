// next.config.ts
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tambahkan konfigurasi Next.js lainnya di sini jika diperlukan
  // Misalnya, untuk optimasi gambar jika Anda menggunakan domain eksternal
};

export default nextConfig;